node_modules/
dist/
coverage/
.vite/
*.tsbuildinfo
*.local
.DS_Store
.idea/
.vscode/
test-results/
playwright-report/
plans/*.plan.json
!plans/.gitkeep
# Scratch workspace for the task runner: ledger, briefs, review packages.
/.superpowers/
# docs/superpowers/ is deliberately NOT ignored — the design spec and the
# implementation plan are committed deliverables, and the README links to them.
